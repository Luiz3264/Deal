# Deal

A simple POS project made from scratch.

## Instructions

Run `npm run start` in the root directory after installing the dependencies with `npm install` to start the web server.

Access the items menu through `/items` (E.g. http://localhost:3000/items)

Access the sales menu through `/sales` (E.g. http://localhost:3000/sales)
