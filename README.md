# Deal

A simple POS made from scratch with React.js, Tailwind CSS, Vite and Typescript.

## Instructions

Use `npm run start` in the root directory after installing the dependencies to start the web server.

The default config password is: `1234`
